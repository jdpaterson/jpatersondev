import styled from 'styled-components'
import {
  background,
  border,
  color,
  flexbox,
  grid,
  layout,
  position,
  space
} from 'styled-system'

export const Box = styled.div`
  ${background}
  ${border}
  ${color}
  ${flexbox}
  ${grid}
  ${layout}
  ${position}
  ${space}
`